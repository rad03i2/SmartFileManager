<Window
    x:Class="SmartFileManager.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Smart File Manager">

    <Grid Padding="24" RowSpacing="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Smart File Manager" FontSize="30" FontWeight="SemiBold" />
            <TextBlock Text="Batch rename files and find duplicates safely." Opacity="0.75" Margin="0,4,0,0" />
        </StackPanel>

        <Grid Grid.Row="1" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" x:Name="RenameButton" Click="RenameButton_Click" HorizontalAlignment="Stretch" Padding="18">
                <StackPanel>
                    <TextBlock Text="Batch Rename" FontSize="20" FontWeight="SemiBold" />
                    <TextBlock Text="Generate a preview and rename selected files." Opacity="0.75" />
                </StackPanel>
            </Button>

            <Button Grid.Column="1" x:Name="DuplicateButton" Click="DuplicateButton_Click" HorizontalAlignment="Stretch" Padding="18">
                <StackPanel>
                    <TextBlock Text="Duplicate Finder" FontSize="20" FontWeight="SemiBold" />
                    <TextBlock Text="Find equal files using size and SHA-256." Opacity="0.75" />
                </StackPanel>
            </Button>
        </Grid>

        <Border Grid.Row="2" Padding="18" CornerRadius="12" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
            <ScrollViewer>
                <TextBlock x:Name="OutputText" TextWrapping="Wrap" FontFamily="Consolas" Text="Ready." />
            </ScrollViewer>
        </Border>
    </Grid>
</Window>
